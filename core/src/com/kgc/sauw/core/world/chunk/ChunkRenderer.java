package com.kgc.sauw.core.world.chunk;

public class ChunkRenderer {
    public static void render(Chunk chunk) {
        for (int x = 0; x < Chunk.CHUCK_SIZE_X; x++) {
            for (int z = 0; z < Chunk.CHUCK_SIZE_Z; z++) {
                
            }
        }
    }
}
